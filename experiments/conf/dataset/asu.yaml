id: asu
description: "ASU First Destination Survey + career outcomes dataset."
manifest_path: "data/manifests/asu_sources.yaml"
artifacts_path: "data/processed/asu/artifacts.jsonl"
# Uses pre-generated fixtures by default for reproducibility
# Run `python -m ads_core.ingest.asu` to regenerate
# Set strict_data: true for paper experiments to require real ingested data
strict_data: false
