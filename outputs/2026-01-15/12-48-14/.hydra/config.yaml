dataset:
  id: toy
  description: Built-in toy dataset (no internet).
embedding:
  kind: stub
  d: 64
lenses:
  mode: identity
objectives:
  enabled:
  - learner
constraints:
  enable_ethics: true
  autonomy_drift_tau: 1.0
planning:
  depth: 2
  topk: 5
outputs:
  base_dir: experiments/reports
  run_id: paper_B1_baseline_learner_only_stub_identity_tau1.0_learner
seed: 42
